import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Bienvenido a las aplicaciones ionic',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
